<% if @bourbon %>
    <h1> View all the reviews for  <%= @bourbon.name %>!</h1>
<% else %>
    <h1> View all the reviews</h1>
<% end %>

<% @reviews.each do |r| %>
    <h2> <%= r.bourbon.name %></h2>
    <h2> <%= r.title %> - <%= r.stars %> </h2>
    <p> <%= r.content %> </p>
<% end %>